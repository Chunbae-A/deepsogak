# CI 스모크 테스트용 최소 의존성. insightface/opencv-python은 일부러 뺐다 — 정렬 단계는
# tests/fakes.py의 FakeAligner로 대체하므로 CI에서 무거운 InsightFace 가중치를 받을 필요가 없다
# (services/faceguard-model-api의 model-api-ci.yml이 FakeSession/FakeFaceEncoder로 진짜 모델
# 없이 테스트하는 것과 동일한 선례).
--index-url https://download.pytorch.org/whl/cpu
torch==2.5.1
torchvision==0.20.1

--index-url https://pypi.org/simple
timm==1.0.11
onnx==1.17.0
onnxruntime==1.23.2
scikit-learn==1.5.2
Pillow==12.3.0
numpy==2.0.2
pyyaml==6.0.2
tqdm==4.67.1
pandas==2.2.3
