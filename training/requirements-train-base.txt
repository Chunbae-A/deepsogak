# torch/torchvision은 별도 파일(requirements-train-cpu.txt / -gpu.txt)에서 설치한다.
#
# insightface/opencv-python/Pillow/numpy/onnxruntime는 services/faceguard-model-api의
# requirements-api-base.txt와 완전히 동일한 버전으로 고정한다 — 정렬(norm_crop)과 리사이즈
# (PIL BILINEAR) 동작이 학습·추론 시점에 비트 단위로 같아야 하기 때문이다.
timm==1.0.11
onnx==1.17.0
onnxruntime==1.23.2
scikit-learn==1.5.2
opencv-python==4.12.0.88
insightface==1.0.1
Pillow==12.3.0
numpy==2.0.2
pyyaml==6.0.2
tqdm==4.67.1
pandas==2.2.3
