"""원본·보호사진 접근 제어 인터페이스 (#76 스캐폴딩, 아직 사용되지 않음).

## 배경

`server/main.py`는 STORAGE_DIR(uploads/protected/saved) 전체를 인증 없이
`/static`으로 서빙한다(#74/#75). DB 파일 노출(#75)은 고쳤지만, 원본·보호
사진 자체는 여전히 jobId(uuid4 hex 12자, 48비트)만 알면 누구나 내려받을
수 있다. jobId는 무작위 추측은 어렵지만, 로그·네트워크 요청·에러 메시지
등으로 하나만 흘러나가도 그 사람의 원본 얼굴 사진이 노출된다.

이 모듈은 아직 main.py 어디에도 연결돼 있지 않다 — 어떤 방식을 쓸지가
코드 문제가 아니라 제품 결정이라 먼저 정해야 한다(아래 옵션 참고).
결정되면 이 인터페이스를 실제로 구현하고 main.py의 /static 마운트와
photoUrl 응답에 연결한다.

## 검토한 옵션

1. **짧게 만료되는 서명된 URL** — job 완료 응답에 만료시각이 박힌 서명을
   포함한 URL을 내려주고, 정적 서빙 대신 서명을 검증하는 엔드포인트를
   추가한다. 클라이언트 코드 변경이 가장 적다(여전히 URL 하나로 이미지를
   받음). 서명 비밀키 관리가 추가로 필요하다.
2. **jobId 대신 예측 불가능한 별도 접근 토큰** — jobId는 지금처럼 API
   응답/로그에 계속 노출되더라도, 실제 이미지 접근에는 별도의 더 긴
   랜덤 토큰을 요구한다. 구현은 간단하지만 "토큰이 뭔가로 유출되면
   만료 전까지 계속 유효"하다는 근본 문제는 옵션 1과 같다.
3. **정적 서빙을 없애고 인증된 API로 바이트를 직접 내려주기** —
   `/static` 마운트를 걷어내고, `GET /api/protection/photos/{jobId}/original`
   같은 엔드포인트가 (세션/토큰 검증 후) 이미지 바이트를 스트리밍한다.
   가장 안전하지만 클라이언트가 `<Image source={{uri: 정적URL}}>` 패턴을
   못 쓰게 될 수 있어 앱 쪽 변경이 가장 크다.

## 결정에 필요한 입력

- 딥소각 서버가 "세션/사용자"를 구분하게 될지(#77과 직접 연관) — 그
  결과에 따라 토큰을 누구 앞으로 발급할지가 달라진다.
- 해커톤 데모(8/21까지) 범위에서 이걸 얼마나 우선할지.

## 이 모듈의 현재 상태

아래 함수 시그니처만 정의돼 있고 전부 NotImplementedError를 던진다.
옵션이 정해지면 하나를 골라 구현하고, 나머지는 지운다.
"""

from __future__ import annotations


def issue_photo_access_token(job_id: str) -> str:
    """job_id의 원본·보호사진에 접근할 수 있는 토큰(또는 서명된 URL 조각)을
    발급한다. 옵션 1/2 중 무엇을 고르느냐에 따라 구현이 달라진다.
    """
    raise NotImplementedError(
        "#76에서 접근 제어 방식(서명 URL / 별도 토큰 / 인증 API)이 정해지면 구현"
    )


def verify_photo_access_token(token: str) -> str | None:
    """토큰을 검증해 유효하면 job_id를, 아니면 None을 돌려준다."""
    raise NotImplementedError(
        "#76에서 접근 제어 방식(서명 URL / 별도 토큰 / 인증 API)이 정해지면 구현"
    )
